var nombre ='Jaime', apellido ='Rios';

var nombreMayuscula = nombre.toUpperCase();
var apellidoMinuscula = apellido.toLowerCase();

var primeraLetraNombre = nombre.charAt(0).toLowerCase();
var cantLetrasNombre = nombre.length;

var nombreCompleto = `${nombre} ${apellido}`;

var str = nombre.substr(1,5)

var count = nombre.length

var ultimaLetra = nombre.charAt(count-1)

var ul = nombre.substr(-1)
